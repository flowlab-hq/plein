[package]
name = "plein-app"
version = "0.1.0"
description = "Plein Mac app — open a .plein and render one viewpoint"
authors = ["Flowlab HQ"]
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "plein_app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
url = "2"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
